# CS2612 2023-2024 Final

1. 给出正则表达式$a(ab)^*b$的NFA, DFA (15')

2. 给出了一个涉及`+`、`-`、`*`、`(`、`)`的语法规则，对一个形如`- ID * (ID + ID)`的表达式进行移入规约分析 (15')

3. 证明或证否：$[[c_2;c_3]].(\text{inf})\subseteq [[c_1;c_2;c_3]].(\text{inf})$ (15')

4. 用BW不动点定理给出$F$在集合包含关系下的最小不动点，$F$的定义如下，其中$X\in P(\mathbb{R})$, $P$表示幂集 (20')
   $$
   F(X):=\{0\}\cup \left\{x\Big|\frac{1+x}{2}, x\in X\right\} \cup \left\{x\Big|\frac{1-x}{2}, x\in X\right\}
   $$

5. 给出下面控制流图的支配边界，并画出支配树 (20')

   $A\rightarrow B\rightarrow C\rightarrow (D/E) \rightarrow F \rightarrow A/G$

6. 对一个不含有分离逻辑的程序进行符号执行，程序难度约为C++前三堂课的内容（15'）